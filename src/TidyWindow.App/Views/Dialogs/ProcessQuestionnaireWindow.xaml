<Window x:Class="TidyWindow.App.Views.Dialogs.ProcessQuestionnaireWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:TidyWindow.App.Converters"
        xmlns:vm="clr-namespace:TidyWindow.App.ViewModels.Dialogs"
        mc:Ignorable="d"
        Title="Process questionnaire"
        Height="600"
        Width="760"
        Background="#0B1120"
        Foreground="#E2E8F0"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:QuestionnaireOptionSelectionConverter x:Key="QuestionnaireOptionSelectionConverter" />
        <Style TargetType="RadioButton" x:Key="QuestionnaireRadioButtonStyle">
            <Setter Property="Foreground" Value="#F8FAFC" />
            <Setter Property="Margin" Value="0,4,0,0" />
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="First-run questionnaire"
                       FontSize="24"
                       FontWeight="SemiBold" />
            <TextBlock Text="Answer a few capability questions so TidyWindow can tailor auto-stop recommendations."
                       Foreground="#94A3B8"
                       Margin="0,4,0,0" />
        </StackPanel>

        <Border Grid.Row="1"
                Background="#0F172A"
                BorderBrush="#1D2A44"
                BorderThickness="1"
                CornerRadius="12"
                Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Questions}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:ProcessQuestionDialogQuestionViewModel}">
                            <Border Background="#111C36"
                                    BorderBrush="#1F2A44"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="16"
                                    Margin="0,0,0,16">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}"
                                                   FontSize="16"
                                                   FontWeight="SemiBold" />
                                        <TextBlock Text="Required"
                                                   Foreground="#F97316"
                                                   Margin="12,0,0,0"
                                                   Visibility="{Binding Required, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding Prompt}"
                                               Foreground="#94A3B8"
                                               Margin="0,4,0,12" />
                                    <ItemsControl ItemsSource="{Binding Options}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Margin="0,6,0,0">
                                                    <RadioButton Content="{Binding Label}"
                                                                 GroupName="{Binding DataContext.Id, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                                 Style="{StaticResource QuestionnaireRadioButtonStyle}"
                                                                    IsChecked="{Binding DataContext.SelectedOptionId,
                                                                                             RelativeSource={RelativeSource AncestorType=ContentPresenter},
                                                                                             Converter={StaticResource QuestionnaireOptionSelectionConverter},
                                                                                             ConverterParameter={Binding Id}}" />
                                                    <TextBlock Text="{Binding Description}"
                                                               Foreground="#94A3B8"
                                                               Margin="24,0,0,0"
                                                               Visibility="{Binding Description, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                               TextWrapping="Wrap" />
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    MinWidth="100"
                    Margin="0,0,12,0"
                    Click="CancelButton_OnClick" />
            <Button Content="Apply recommendations"
                    MinWidth="180"
                    Background="#7C3AED"
                    Foreground="White"
                    Click="RunButton_OnClick"
                    IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
