<Page x:Class="TidyWindow.App.Views.PathPilotPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="clr-namespace:TidyWindow.App.Views"
    xmlns:converters="clr-namespace:TidyWindow.App.Converters"
    xmlns:vm="clr-namespace:TidyWindow.App.ViewModels"
    xmlns:ux="clr-namespace:TidyWindow.App.Controls"
      Title="PathPilot"
      mc:Ignorable="d"
      Background="Transparent"
      UseLayoutRounding="True"
      SnapsToDevicePixels="True"
      TextOptions.TextFormattingMode="Display"
      TextOptions.TextRenderingMode="ClearType"
      TextOptions.TextHintingMode="Fixed"
      RenderOptions.BitmapScalingMode="HighQuality">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:PathPilotStatusSeverityToBrushConverter x:Key="SeverityBrush" />

        <SolidColorBrush x:Key="CardBackground" Color="#0C1729" />
        <SolidColorBrush x:Key="CardBorder" Color="#1E2A3D" />
        <SolidColorBrush x:Key="BadgeForeground" Color="#F8FAFC" />

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="UseLayoutRounding" Value="True" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#3A6FA8" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="#0B1220" />
            <Setter Property="Background" Value="#34D399" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="#E2E8F0" />
            <Setter Property="Background" Value="#142033" />
            <Setter Property="BorderBrush" Value="#26344D" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1B2C48" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryToggleButton" TargetType="ToggleButton">
            <Setter Property="Foreground" Value="#E2E8F0" />
            <Setter Property="Background" Value="#142033" />
            <Setter Property="BorderBrush" Value="#26344D" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1B2C48" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#1B2C48" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Background" Value="#0F172A" />
        </Style>

        <DataTemplate x:Key="RuntimeCardTemplate" DataType="{x:Type vm:PathPilotRuntimeCardViewModel}">
            <Border Style="{StaticResource CardStyle}" MinWidth="440" MaxWidth="760" Padding="22">
                <StackPanel>
                    <StackPanel.Resources>
                        <Style TargetType="FrameworkElement">
                            <Setter Property="Margin" Value="0,0,0,12" />
                        </Style>
                    </StackPanel.Resources>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <StackPanel.Resources>
                            <Style TargetType="FrameworkElement">
                                <Setter Property="Margin" Value="0,0,12,0" />
                            </Style>
                        </StackPanel.Resources>
                        <TextBlock Text="{Binding FriendlyName}"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="#F8FAFC"
                                   TextTrimming="CharacterEllipsis"
                                   MaxWidth="340" />
                        <ItemsControl ItemsSource="{Binding StatusBadges}" Margin="10,0,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource BadgeStyle}"
                                            Background="{Binding Severity, Converter={StaticResource SeverityBrush}}">
                                        <TextBlock Text="{Binding Label}"
                                                   Foreground="{StaticResource BadgeForeground}"
                                                   FontSize="11"
                                                   FontWeight="SemiBold" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                        <StackPanel.Resources>
                            <Style TargetType="FrameworkElement">
                                <Setter Property="Margin" Value="0,0,12,0" />
                            </Style>
                        </StackPanel.Resources>
                        <Border Style="{StaticResource BadgeStyle}" Background="#0B2538">
                            <TextBlock Text="{Binding VersionChipLabel}" Foreground="#7DD3FC" FontSize="12" FontWeight="SemiBold" />
                        </Border>
                    </StackPanel>

                    <Border Background="#0F172A" BorderBrush="#1F2E46" BorderThickness="1" CornerRadius="12" Padding="14" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock Text="Current version"
                                       Foreground="#9CA3AF"
                                       FontWeight="SemiBold"
                                       FontSize="12" />
                            <TextBlock Text="{Binding VersionChipLabel}" Foreground="#E2E8F0" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>

                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Content="Switch" Style="{StaticResource PrimaryButton}" MinWidth="96" HorizontalAlignment="Stretch" Margin="0,0,10,0"
                                Command="{Binding DataContext.ShowInstallationsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                CommandParameter="{Binding}" IsEnabled="{Binding HasInstallations}" />
                        <Button Grid.Column="1" Content="Details" Style="{StaticResource SecondaryButton}" MinWidth="96" HorizontalAlignment="Stretch" Margin="0,0,10,0"
                                Command="{Binding DataContext.ShowDetailsCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                CommandParameter="{Binding}" />
                        <Button Grid.Column="2" Content="Order" Style="{StaticResource SecondaryButton}" MinWidth="96" HorizontalAlignment="Stretch"
                                Command="{Binding DataContext.ShowResolutionOrderCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                CommandParameter="{Binding}" IsEnabled="{Binding HasResolutionOrder}" />
                    </Grid>
                </StackPanel>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="InstallationTemplate" DataType="{x:Type vm:PathPilotInstallationViewModel}">
            <Border Padding="18,16" CornerRadius="12" BorderThickness="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#1E293B" />
                        <Setter Property="BorderBrush" Value="#334155" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter Property="Background" Value="#0F2318" />
                                <Setter Property="BorderBrush" Value="#166534" />
                            </DataTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#475569" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel VerticalAlignment="Center">
                        <!-- Header row: Version + Active badge -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="{Binding VersionDisplay}" FontSize="15" FontWeight="SemiBold" Foreground="#F1F5F9" VerticalAlignment="Center" />
                            <Border Background="#14532D" CornerRadius="6" Padding="8,4" Margin="12,0,0,0" VerticalAlignment="Center"
                                    Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="Active" Foreground="#4ADE80" FontSize="11" FontWeight="SemiBold" />
                            </Border>
                        </StackPanel>

                        <!-- Directory path -->
                        <TextBlock Text="{Binding Directory}" Foreground="#64748B" FontSize="12" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" Margin="0,0,0,10" />

                        <!-- Badges row -->
                        <StackPanel Orientation="Horizontal">
                            <Border Background="#1E3A5F" CornerRadius="6" Padding="8,4" Margin="0,0,8,0">
                                <TextBlock Text="{Binding Source}" Foreground="#7DD3FC" FontSize="11" />
                            </Border>
                            <Border Background="#2E1065" CornerRadius="6" Padding="8,4">
                                <TextBlock Text="{Binding Architecture}" Foreground="#C4B5FD" FontSize="11" />
                            </Border>
                        </StackPanel>

                        <!-- Notes (if any) -->
                        <Border Background="#1C1917" BorderBrush="#44403C" BorderThickness="1" CornerRadius="8" Padding="10,8" Margin="0,12,0,0"
                                Visibility="{Binding HasNotes, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="{Binding NotesDisplay}" Foreground="#FCD34D" FontSize="11" TextWrapping="Wrap" />
                        </Border>
                    </StackPanel>

                    <!-- Action button -->
                    <Button Grid.Column="1" Content="Make active" Style="{StaticResource SecondaryButton}" 
                            Padding="16,10" MinWidth="120" VerticalAlignment="Center" Margin="20,0,0,0"
                            Command="{Binding DataContext.SwitchRuntimeCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                            CommandParameter="{Binding SwitchRequest}" IsEnabled="{Binding CanSwitch}" />
                </Grid>
            </Border>
        </DataTemplate>
    </Page.Resources>

    <Grid>
          <Grid x:Name="PageContentGrid"
              MinWidth="0"
              MaxWidth="1520"
              HorizontalAlignment="Center"
              Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ScrollViewer Grid.RowSpan="2" VerticalScrollBarVisibility="Auto" Padding="4,0,4,24">
                <StackPanel Orientation="Vertical">
                    <controls:PageTitleBar
                                   Title="PathPilot"
                                   Subtitle="Runtime PATH switchboard"
                                   IconGlyph="🛰️"
                                   BadgeText="{Binding LastRefreshedDisplay}"
                                   Margin="0,0,0,18">
                        <controls:PageTitleBar.TrailingContent>
                            <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                                <Button Content="Refresh" Style="{StaticResource PrimaryButton}" Command="{Binding RefreshCommand}" Margin="0,0,20,0" />
                                <Button Content="Export JSON" Style="{StaticResource SecondaryButton}" Command="{Binding ExportJsonCommand}" Margin="0,0,20,0" />
                                <Button Content="Export Markdown" Style="{StaticResource SecondaryButton}" Command="{Binding ExportMarkdownCommand}" />
                            </StackPanel>
                        </controls:PageTitleBar.TrailingContent>
                        <controls:PageTitleBar.BodyContent>
                            <Border Style="{StaticResource CardStyle}" Padding="20" Background="#0F1F35">
                                <StackPanel>
                                    <StackPanel.Resources>
                                        <Style TargetType="FrameworkElement">
                                            <Setter Property="Margin" Value="0,0,0,8" />
                                        </Style>
                                    </StackPanel.Resources>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <StackPanel.Resources>
                                            <Style TargetType="FrameworkElement">
                                                <Setter Property="Margin" Value="0,0,10,0" />
                                            </Style>
                                        </StackPanel.Resources>
                                        <TextBlock Text="PathPilot" FontSize="18" FontWeight="SemiBold" Foreground="#F8FAFC" />
                                        <Button Content="Details" Style="{StaticResource SecondaryButton}" Padding="12,6" Margin="8,0,0,0" VerticalAlignment="Center"
                                                Command="{Binding ShowPageInfoCommand}" />
                                    </StackPanel>
                                    <TextBlock Text="Switch runtimes that own PATH without the clutter."
                                               Foreground="#CBD5E1" Margin="0,6,0,0" />
                                    <StackPanel Visibility="{Binding ShowPathPilotHero, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <StackPanel.Resources>
                                            <Style TargetType="FrameworkElement">
                                                <Setter Property="Margin" Value="0,0,0,4" />
                                            </Style>
                                        </StackPanel.Resources>
                                        <TextBlock Text="PathPilot updates HKLM + machine PATH, affecting all accounts." Foreground="#E2E8F0" FontSize="12" />
                                        <TextBlock Text="Snapshot is taken before edits; confirm destinations before switching." Foreground="#A5B4FC" FontSize="11" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </controls:PageTitleBar.BodyContent>
                    </controls:PageTitleBar>

                    <StackPanel x:Name="MainLayoutHost" Margin="0,0,0,8" Orientation="Vertical">
                    <StackPanel.Resources>
                        <Style TargetType="FrameworkElement">
                            <Setter Property="Margin" Value="0,0,0,20" />
                        </Style>
                    </StackPanel.Resources>
                    <Border Style="{StaticResource CardStyle}" Background="#102038" Padding="22" Margin="0,0,0,6">
                        <StackPanel>
                            <StackPanel.Resources>
                                <Style TargetType="FrameworkElement">
                                    <Setter Property="Margin" Value="0,0,0,8" />
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="Switch without surprises" FontSize="15" FontWeight="SemiBold" Foreground="#F8FAFC" />
                            <TextBlock Text="Review a runtime, then switch. Click Details for the deeper view." Foreground="#A5B4FC" FontSize="12" />
                        </StackPanel>
                    </Border>

                    <ListBox x:Name="RuntimesList"
                             ItemsSource="{Binding PagedRuntimes}"
                             Loaded="OnRuntimesLoaded"
                             Background="Transparent"
                             BorderThickness="0"
                             Foreground="#F8FAFC"
                             ItemTemplate="{StaticResource RuntimeCardTemplate}"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.CanContentScroll="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             SelectionMode="Single">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ux:VirtualizingAdaptiveTilePanel MinColumnWidth="440" MaxColumnWidth="780" MinColumns="1" MaxColumns="3" ColumnSpacing="28" RowSpacing="24" />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Focusable" Value="False" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>

                    <Border>
                        <Border.Style>
                            <Style TargetType="Border" BasedOn="{StaticResource CardStyle}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasRuntimeData}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <StackPanel.Resources>
                                <Style TargetType="FrameworkElement">
                                    <Setter Property="Margin" Value="0,0,0,6" />
                                </Style>
                            </StackPanel.Resources>
                            <TextBlock Text="No runtime inventory yet" FontSize="14" FontWeight="SemiBold" Foreground="#F8FAFC" />
                            <TextBlock Text="Run a scan to capture Python, JDK, Node, and .NET footprints before switching." Foreground="#94A3B8" FontSize="12" TextWrapping="Wrap" />
                            <Button Content="Scan runtimes" Style="{StaticResource PrimaryButton}" Width="150" Command="{Binding RefreshCommand}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Visibility="{Binding HasWarnings, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel>
                            <TextBlock Text="Warnings" FontSize="14" FontWeight="SemiBold" Foreground="#F8FAFC" Margin="0,0,0,14" />
                            <ItemsControl ItemsSource="{Binding Warnings}">
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Margin" Value="0,0,0,10" />
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#1C1212" BorderBrush="#7F1D1D" BorderThickness="1" CornerRadius="10" Padding="14,12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="⚠" Foreground="#F87171" FontSize="14" VerticalAlignment="Top" Margin="0,1,10,0" />
                                                <TextBlock Grid.Column="1" Text="{Binding}" Foreground="#FCA5A5" TextWrapping="Wrap" FontSize="13" LineHeight="20" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                                Visibility="{Binding HasMultiplePages, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel.Resources>
                            <Style TargetType="FrameworkElement">
                                <Setter Property="Margin" Value="0,0,10,0" />
                            </Style>
                        </StackPanel.Resources>
                        <TextBlock Text="{Binding PageDisplay}" Foreground="#C6D4EE" FontSize="12" VerticalAlignment="Center" />
                        <Button Content="Prev" Style="{StaticResource SecondaryButton}" Command="{Binding GoToPreviousPageCommand}" IsEnabled="{Binding CanGoToPreviousPage}" />
                        <Button Content="Next" Style="{StaticResource SecondaryButton}" Command="{Binding GoToNextPageCommand}" IsEnabled="{Binding CanGoToNextPage}" />
                    </StackPanel>
                </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Grid Background="#880B172A" Visibility="{Binding IsInventoryLoading, Converter={StaticResource BooleanToVisibilityConverter}}" Panel.ZIndex="10">
            <Border Background="#0F172A" Padding="28,24" CornerRadius="14" BorderBrush="#334155" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Collecting runtime inventory..." Foreground="#E2E8F0" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,14" />
                    <ProgressBar IsIndeterminate="True" Height="5" Width="200" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Background="#880B172A" Visibility="{Binding IsSwitchingPath, Converter={StaticResource BooleanToVisibilityConverter}}" Panel.ZIndex="16">
            <Border Background="#0F172A" Padding="28,24" CornerRadius="14" BorderBrush="#334155" BorderThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel HorizontalAlignment="Center">
                    <TextBlock Text="Switching PATH winner..." Foreground="#E2E8F0" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,14" />
                    <ProgressBar IsIndeterminate="True" Height="5" Width="180" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Background="#B30B172A" Visibility="{Binding IsMachineScopeWarningOpen, Converter={StaticResource BooleanToVisibilityConverter}}" Panel.ZIndex="14">
            <Border Background="#0D1A2F" Padding="32,28" CornerRadius="16" BorderBrush="#334155" BorderThickness="1" MaxWidth="520" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <Grid Margin="0,0,0,18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Background="#1E293B" CornerRadius="10" Width="44" Height="44" VerticalAlignment="Top">
                            <TextBlock Text="⚙" FontSize="20" Foreground="#FBBF24" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" Margin="16,0,0,0">
                            <TextBlock Text="Machine-wide change" FontSize="17" FontWeight="SemiBold" Foreground="#F8FAFC" Margin="0,0,0,6" />
                            <TextBlock Text="This will update HKLM and the system PATH, affecting all Windows accounts on this machine." Foreground="#94A3B8" TextWrapping="Wrap" FontSize="13" LineHeight="20" />
                        </StackPanel>
                    </Grid>
                    <Border Background="#1E1B4B" BorderBrush="#3730A3" BorderThickness="1" CornerRadius="10" Padding="14,12" Margin="0,0,0,22">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="ℹ" Foreground="#A5B4FC" FontSize="14" VerticalAlignment="Top" Margin="0,1,10,0" />
                            <TextBlock Grid.Column="1" Text="Proceed only if you trust the destination you're promoting." Foreground="#C7D2FE" TextWrapping="Wrap" FontSize="13" />
                        </Grid>
                    </Border>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="1" Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelMachineScopeWarningCommand}" MinWidth="100" Margin="0,0,12,0" />
                        <Button Grid.Column="2" Content="Switch now" Style="{StaticResource PrimaryButton}" Command="{Binding DismissMachineScopeWarningCommand}" MinWidth="120" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Background="#880B172A" Visibility="{Binding IsInstallationsDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}" Panel.ZIndex="12">
            <Border Background="#0F172A" Padding="32,28" CornerRadius="16" BorderBrush="#334155" BorderThickness="1" MaxWidth="720" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Background="#1E293B" CornerRadius="10" Width="44" Height="44" VerticalAlignment="Top">
                            <TextBlock Text="⇄" FontSize="18" Foreground="#38BDF8" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" Margin="16,0,0,0">
                            <TextBlock Text="Available PATH targets" FontSize="17" FontWeight="SemiBold" Foreground="#F8FAFC" Margin="0,0,0,6" />
                            <TextBlock Text="Pick the installation to promote. PathPilot will back up the current PATH entry." Foreground="#94A3B8" FontSize="13" TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                    <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                        <ItemsControl ItemsSource="{Binding InstallationsDialogRuntime.Installations}" ItemTemplate="{StaticResource InstallationTemplate}">
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Margin" Value="0,0,0,10" />
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                        </ItemsControl>
                    </ScrollViewer>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="1" Content="Close" Style="{StaticResource SecondaryButton}" Command="{Binding CloseInstallationsCommand}" MinWidth="100" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Background="#880B172A" Visibility="{Binding IsDetailsDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}" Panel.ZIndex="12">
            <Border Background="#0F172A" Padding="32,28" CornerRadius="16" BorderBrush="#334155" BorderThickness="1" MaxWidth="720" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <Grid Margin="0,0,0,18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Background="#1E293B" CornerRadius="10" Width="44" Height="44" VerticalAlignment="Top">
                            <TextBlock Text="📋" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" Margin="16,0,0,0">
                            <TextBlock Text="Runtime details" FontSize="17" FontWeight="SemiBold" Foreground="#F8FAFC" Margin="0,0,0,6" />
                            <TextBlock Text="{Binding DetailsDialogRuntime.Summary}" Foreground="#94A3B8" FontSize="13" TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                    <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="12" Padding="16,14" Margin="0,0,0,18">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Active executable" FontWeight="SemiBold" Foreground="#64748B" FontSize="12" Margin="0,0,20,8" />
                            <TextBlock Grid.Column="1" Text="{Binding DetailsDialogRuntime.ActiveExecutablePath}" Foreground="#E2E8F0" TextWrapping="Wrap" FontSize="13" Margin="0,0,0,8" />
                            <TextBlock Grid.Row="1" Text="PATH entry" FontWeight="SemiBold" Foreground="#64748B" FontSize="12" Margin="0,0,20,0" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DetailsDialogRuntime.PathEntry}" Foreground="#E2E8F0" TextWrapping="Wrap" FontSize="13" />
                        </Grid>
                    </Border>
                    <TextBlock Text="Installations" FontWeight="SemiBold" Foreground="#94A3B8" FontSize="13" Margin="0,0,0,12" />
                    <ScrollViewer MaxHeight="280" VerticalScrollBarVisibility="Auto" Margin="0,0,0,18">
                        <ItemsControl ItemsSource="{Binding DetailsDialogRuntime.Installations}" ItemTemplate="{StaticResource InstallationTemplate}">
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Margin" Value="0,0,0,10" />
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                        </ItemsControl>
                    </ScrollViewer>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="1" Content="Close" Style="{StaticResource SecondaryButton}" Command="{Binding CloseDetailsCommand}" MinWidth="100" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Background="#880B172A" Visibility="{Binding IsResolutionDialogOpen, Converter={StaticResource BooleanToVisibilityConverter}}" Panel.ZIndex="12">
            <Border Background="#0F172A" Padding="32,28" CornerRadius="16" BorderBrush="#334155" BorderThickness="1" MaxWidth="600" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Background="#1E293B" CornerRadius="10" Width="44" Height="44" VerticalAlignment="Top">
                            <TextBlock Text="≡" FontSize="22" Foreground="#A78BFA" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" Margin="16,0,0,0">
                            <TextBlock Text="PATH resolution order" FontSize="17" FontWeight="SemiBold" Foreground="#F8FAFC" Margin="0,0,0,6" />
                            <TextBlock Text="How Windows resolves executables from PATH entries." Foreground="#94A3B8" FontSize="13" TextWrapping="Wrap" />
                        </StackPanel>
                    </Grid>
                    <ScrollViewer MaxHeight="320" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                        <ItemsControl ItemsSource="{Binding ResolutionDialogRuntime.ResolutionOrder}">
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Margin" Value="0,0,0,8" />
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#1E293B" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="14,12">
                                        <TextBlock Text="{Binding}" Foreground="#E2E8F0" TextWrapping="Wrap" FontSize="13" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="1" Content="Close" Style="{StaticResource SecondaryButton}" Command="{Binding CloseResolutionOrderCommand}" MinWidth="100" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Background="#880B172A" Visibility="{Binding IsPageInfoOpen, Converter={StaticResource BooleanToVisibilityConverter}}" Panel.ZIndex="13">
            <Border Background="#0F172A" Padding="32,28" CornerRadius="16" BorderBrush="#334155" BorderThickness="1" MaxWidth="580" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Background="#1E293B" CornerRadius="10" Width="44" Height="44" VerticalAlignment="Top">
                            <TextBlock Text="ℹ" FontSize="20" Foreground="#38BDF8" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <StackPanel Grid.Column="1" Margin="16,0,0,0">
                            <TextBlock Text="About PathPilot" FontSize="17" FontWeight="SemiBold" Foreground="#F8FAFC" Margin="0,0,0,6" />
                            <TextBlock Text="Promotes one runtime to the machine PATH so every user session resolves that toolchain consistently." Foreground="#94A3B8" FontSize="13" TextWrapping="Wrap" LineHeight="20" />
                        </StackPanel>
                    </Grid>
                    <TextBlock Text="How it works" FontWeight="SemiBold" Foreground="#94A3B8" FontSize="13" Margin="0,0,0,14" />
                    <StackPanel Margin="0,0,0,18">
                        <Border Background="#1E293B" CornerRadius="10" Padding="14,12" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Background="#334155" CornerRadius="6" Width="24" Height="24" VerticalAlignment="Top">
                                    <TextBlock Text="1" Foreground="#E2E8F0" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="Inventory finds installed runtimes (Python, .NET, Node, JDK, etc.)." Foreground="#CBD5E1" FontSize="13" TextWrapping="Wrap" Margin="12,2,0,0" />
                            </Grid>
                        </Border>
                        <Border Background="#1E293B" CornerRadius="10" Padding="14,12" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Background="#334155" CornerRadius="6" Width="24" Height="24" VerticalAlignment="Top">
                                    <TextBlock Text="2" Foreground="#E2E8F0" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="Switch lets you pick an installation to promote; PathPilot backs up the prior PATH entry." Foreground="#CBD5E1" FontSize="13" TextWrapping="Wrap" Margin="12,2,0,0" />
                            </Grid>
                        </Border>
                        <Border Background="#1E293B" CornerRadius="10" Padding="14,12" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Background="#334155" CornerRadius="6" Width="24" Height="24" VerticalAlignment="Top">
                                    <TextBlock Text="3" Foreground="#E2E8F0" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="Details shows active executables, PATH entries, and installation sources." Foreground="#CBD5E1" FontSize="13" TextWrapping="Wrap" Margin="12,2,0,0" />
                            </Grid>
                        </Border>
                        <Border Background="#1E293B" CornerRadius="10" Padding="14,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Background="#334155" CornerRadius="6" Width="24" Height="24" VerticalAlignment="Top">
                                    <TextBlock Text="4" Foreground="#E2E8F0" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="Resolution order lists how PATH will resolve if multiple installs exist." Foreground="#CBD5E1" FontSize="13" TextWrapping="Wrap" Margin="12,2,0,0" />
                            </Grid>
                        </Border>
                    </StackPanel>
                    <Border Background="#1E1B4B" BorderBrush="#3730A3" BorderThickness="1" CornerRadius="10" Padding="14,12" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🛡" FontSize="14" VerticalAlignment="Top" Margin="0,1,10,0" />
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Safety" FontWeight="SemiBold" Foreground="#C7D2FE" FontSize="13" Margin="0,0,0,4" />
                                <TextBlock Text="Changes are machine-wide (HKLM + system PATH). A snapshot is taken before switching; you can revert via activity log guidance." Foreground="#A5B4FC" FontSize="12" TextWrapping="Wrap" LineHeight="18" />
                            </StackPanel>
                        </Grid>
                    </Border>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="1" Content="Close" Style="{StaticResource SecondaryButton}" Command="{Binding ClosePageInfoCommand}" MinWidth="100" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
