<Page x:Class="TidyWindow.App.Views.BootstrapPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Bootstrap"
      Background="Transparent">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Border Width="48"
                    Height="48"
                    CornerRadius="12"
                    Background="#1E293B"
                    Margin="0,0,12,0">
                <TextBlock Text="🚀"
                           FontSize="24"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
            <StackPanel>
                <TextBlock Text="Bootstrap"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="#F8FAFC"/>
                <TextBlock Text="Verify installers and prep your environment"
                           FontSize="14"
                           Foreground="#94A3B8"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1"
                Margin="0,24,0,20"
                Padding="16"
                Background="#111827"
                CornerRadius="12">
            <DockPanel VerticalAlignment="Center">
                <StackPanel Margin="0,0,18,0"
                            DockPanel.Dock="Left">
                    <TextBlock Text="Include package managers"
                               FontSize="14"
                               Foreground="#CBD5F5"/>
                    <TextBlock Text="Toggle optional sources before running detection"
                               FontSize="12"
                               Foreground="#64748B"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal"
                            Margin="0,0,18,0"
                            DockPanel.Dock="Left">
                    <CheckBox Content="Chocolatey"
                              FontSize="13"
                              Foreground="#F8FAFC"
                              Margin="0,0,12,0"
                              IsChecked="{Binding IncludeChocolatey, Mode=TwoWay}"/>
                    <CheckBox Content="Scoop"
                              FontSize="13"
                              Foreground="#F8FAFC"
                              IsChecked="{Binding IncludeScoop, Mode=TwoWay}"/>
                </StackPanel>
                <Button Content="Run detection"
                        Background="#22C55E"
                        Foreground="#06111D"
                        FontWeight="Semibold"
                        HorizontalAlignment="Right"
                        Command="{Binding DetectCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="IsEnabled">
                                <Setter.Value>
                                    <Binding Path="IsBusy"
                                             Converter="{StaticResource InverseBooleanConverter}"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Cursor"
                                    Value="Hand"/>
                            <Setter Property="BorderThickness"
                                    Value="0"/>
                            <Setter Property="Padding"
                                    Value="16,8"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsEnabled"
                                                     Value="False">
                                                <Setter Property="Opacity"
                                                        Value="0.6"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver"
                                                     Value="True">
                                                <Setter Property="Background"
                                                        Value="#16A34A"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </DockPanel>
        </Border>

        <Border Grid.Row="2"
                Background="#111827"
                CornerRadius="12"
                Padding="0">
            <Grid VerticalAlignment="Stretch"
                  Margin="20,16,20,16"
                  Grid.IsSharedSizeScope="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition SharedSizeGroup="PackageName"
                                          Width="*"/>
                        <ColumnDefinition SharedSizeGroup="Status"
                                          Width="Auto"/>
                        <ColumnDefinition SharedSizeGroup="Notes"
                                          Width="2*"/>
                        <ColumnDefinition SharedSizeGroup="Actions"
                                          Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Package manager"
                               Foreground="#CBD5F5"
                               FontSize="13"/>
                    <TextBlock Grid.Column="1"
                               Text="Status"
                               Foreground="#CBD5F5"
                               FontSize="13"
                               HorizontalAlignment="Center"
                               Margin="16,0,16,0"/>
                    <TextBlock Grid.Column="2"
                               Text="Notes"
                               Foreground="#CBD5F5"
                               FontSize="13"
                               Margin="16,0,16,0"/>
                    <TextBlock Grid.Column="3"
                               Text="Actions"
                               Foreground="#CBD5F5"
                               FontSize="13"
                               HorizontalAlignment="Left"
                               Margin="16,0,0,0"/>
                </Grid>

                <ListView Grid.Row="1"
                          ItemsSource="{Binding Managers}"
                          Background="Transparent"
                          BorderThickness="0"
                          Foreground="#F8FAFC"
                          Margin="0,12,0,0"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition SharedSizeGroup="PackageName"
                                                      Width="*"/>
                                    <ColumnDefinition SharedSizeGroup="Status"
                                                      Width="Auto"/>
                                    <ColumnDefinition SharedSizeGroup="Notes"
                                                      Width="2*"/>
                                    <ColumnDefinition SharedSizeGroup="Actions"
                                                      Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}"/>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding IsInstalled, Converter={StaticResource BooleanToYesNoConverter}}"
                                           HorizontalAlignment="Center"
                                           Margin="16,0,16,0"/>
                                <TextBlock Grid.Column="2"
                                           Text="{Binding Notes}"
                                           Margin="16,0,16,0"/>
                                <Button Grid.Column="3"
                                        Content="Install or repair"
                                        Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                        CommandParameter="{Binding}"
                                        Background="#7C3AED"
                                        Foreground="#F8FAFC"
                                        FontSize="12"
                                        BorderThickness="0"
                                        Padding="12,6"
                                        Cursor="Hand"
                                        HorizontalAlignment="Left"
                                        Margin="16,0,0,0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="6"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                              VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsEnabled"
                                                                     Value="False">
                                                                <Setter Property="Opacity"
                                                                        Value="0.5"/>
                                                            </Trigger>
                                                            <Trigger Property="IsMouseOver"
                                                                     Value="True">
                                                                <Setter Property="Background"
                                                                        Value="#6D28D9"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.Style>
                        <Style TargetType="ListView">
                            <Setter Property="IsEnabled">
                                <Setter.Value>
                                    <Binding Path="IsBusy"
                                             Converter="{StaticResource InverseBooleanConverter}"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="ItemContainerStyle">
                                <Setter.Value>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment"
                                                Value="Stretch"/>
                                        <Setter Property="Padding"
                                                Value="0"/>
                                        <Setter Property="Margin"
                                                Value="0"/>
                                        <Setter Property="Background"
                                                Value="Transparent"/>
                                        <Setter Property="BorderThickness"
                                                Value="0"/>
                                    </Style>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.Style>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</Page>
