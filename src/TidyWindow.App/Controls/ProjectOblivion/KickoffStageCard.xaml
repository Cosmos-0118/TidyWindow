<UserControl x:Class="TidyWindow.App.Controls.ProjectOblivion.KickoffStageCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:TidyWindow.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:ProjectOblivionTimelineStageViewModel}">
    <Border Style="{StaticResource PopupCardStyle}" Margin="0,0,0,18">
        <StackPanel>
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Text="🚀" FontSize="30" Margin="0,0,12,0"/>
                    <StackPanel>
                        <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold" Foreground="#F8FAFC"/>
                        <TextBlock Text="{Binding Description}" Foreground="#94A3B8" FontSize="13" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
                <Border DockPanel.Dock="Right"
                        Padding="10,4"
                        CornerRadius="8"
                        HorizontalAlignment="Right"
                        Margin="12,0,0,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#1F2937"/>
                            <Setter Property="BorderBrush" Value="#334155"/>
                            <Setter Property="BorderThickness" Value="1"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Active">
                                    <Setter Property="Background" Value="#0F172A"/>
                                    <Setter Property="BorderBrush" Value="#38BDF8"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Completed">
                                    <Setter Property="Background" Value="#052016"/>
                                    <Setter Property="BorderBrush" Value="#22C55E"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Failed">
                                    <Setter Property="Background" Value="#2B0E12"/>
                                    <Setter Property="BorderBrush" Value="#F87171"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding Status}" Foreground="#E2E8F0" FontWeight="SemiBold" FontSize="12"/>
                </Border>
            </DockPanel>

            <Separator Margin="0,14,0,14"/> 

            <TextBlock Text="Initialize the uninstall context, capture inventory data, and queue safety checkpoints before moving on."
                       Foreground="#CBD5F5" FontSize="12" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding Detail}" Foreground="#E2E8F0" FontSize="13" FontWeight="SemiBold" Margin="0,8,0,0" TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</UserControl>
